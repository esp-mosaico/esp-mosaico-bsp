cmake_minimum_required(VERSION 3.16)
if(NOT MOSAICO_UTILS_ROOT)
    set(MOSAICO_UTILS_ROOT "${CMAKE_CURRENT_LIST_DIR}/../../../esp-mosaico-utils")
endif()
if(NOT MOSAICO_BSP_ROOT)
    set(MOSAICO_BSP_ROOT "${CMAKE_CURRENT_LIST_DIR}/../..")
endif()
if(NOT RAYLIB_LITE_ENGINE_ROOT)
    set(RAYLIB_LITE_ENGINE_ROOT "${CMAKE_CURRENT_LIST_DIR}/../../../raylib-lite-engine")
endif()

if(NOT SDKCONFIG)
    set(SDKCONFIG "${CMAKE_BINARY_DIR}/sdkconfig")
endif()
if(NOT SDKCONFIG_DEFAULTS)
    set(SDKCONFIG_DEFAULTS "sdkconfig.defaults;sdkconfig.application.defaults")
endif()
include("${MOSAICO_UTILS_ROOT}/mosaico-tools/cmake/raylib_lite_engine.cmake")
mosaico_game_sdk_configure_gsp_compiler()
mosaico_game_sdk_add_components(RAYLIB AUDIO FX SAVE)
include("${MOSAICO_UTILS_ROOT}/esp-mosaico-recovery/cmake/mosaico_idf_project.cmake")
project(sky_hop VERSION 0.1.0)

include("${MOSAICO_UTILS_ROOT}/mosaico-tools/cmake/system_update.cmake")
