dependencies:
  idf:
    version: ">=6.2"

  esp-mosaico-bsp:
    path: ../../../components/esp-mosaico-bsp

  mosaico_module_camera:
    path: ../../../components/mosaico_module_camera

  espressif/esp_new_jpeg:
    version: "^1.0.0"

  # BSP audio.c uses the 2.x ES8311 configuration API.
  # Keep this direct requirement so an older transitive lock cannot select 1.6.x.
  espressif/esp_codec_dev:
    version: "2.0.0-beta5"
